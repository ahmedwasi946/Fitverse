{
  "name": "FitVerse AI (Java 21 + MySQL)",
  "image": "mcr.microsoft.com/devcontainers/java:21",
  "forwardPorts": [8080, 3306, 5500],
  "portsAttributes": {
    "8080": { "label": "Backend API", "onAutoForward": "notify" },
    "5500": { "label": "Frontend (python http.server)", "onAutoForward": "openBrowser" },
    "3306": { "label": "MySQL", "onAutoForward": "silent" }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mysql-server && sudo service mysql start && sudo mysql -e \"ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root'; CREATE DATABASE IF NOT EXISTS fitverse_db; FLUSH PRIVILEGES;\"",
  "postStartCommand": "sudo service mysql start"
}
